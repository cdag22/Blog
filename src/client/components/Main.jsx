import React from 'react';

import Post from './Post.jsx';

const Main = ({ }) => (
  <div>
    <div className="jumbotron page-title-container">
      <h2 className="page-title">Blog</h2>
    </div>
  </div>
);

export default Main;